---
author: 文永达
pubDatetime: 2025-07-17T22:25:54.547Z
title: 2025.07.17
featured: false
ogImage: https://github.com/satnaing/astro-paper/assets/53733092/1ef0cf03-8137-4d67-ac81-84a032119e3a
tags:
  - release
description: "我的日记"
---

# 2025-07-17

## 上午


​	今天上午我帮组里的同事（大哥）排查了下之前帮他做的程序（Excel的扩展vsto项目，Winform技术栈） 访问远程服务器（Windows 10）上的网络共享位置目录下的两个Excel模板不定时无法访问的问题，通过C# 通过 Process 调用 cmd.exe 使用net命令先登录到有权限对该网络共享位置访问的用户，然后下载文件到本地指定的目录下为D://scb，解决了这个问题，但后续自己测试，如果远程服务器上的网络共享位置目录下的Excel模版文件内容发生变动后，本地是否可以更新，文件看似是更新了，但是通过OLEDB调用会报错，解决该问题需要先删除本地的原文件，再重新从远程服务器上下载新的文件。

```c#
using System;
using System.Diagnostics;
using System.IO;
using System.Runtime.InteropServices;
using System.Security.Principal;

public class NetworkFileHelper
{
    public static bool connectState(string path)
    {
        return connectState(path, "", "");
    }
    /// <summary>
    /// 连接远程共享文件夹
    /// </summary>
    /// <param name="path">远程共享文件夹的路径</param>
    /// <param name="userName">用户名</param>
    /// <param name="passWord">密码</param>
    /// <returns></returns>
    public static bool connectState(string path, string userName, string passWord)
    {
        bool Flag = false;
        Process proc = new Process();
        try
        {
            proc.StartInfo.FileName = "cmd.exe";
            proc.StartInfo.UseShellExecute = false;
            proc.StartInfo.RedirectStandardInput = true;
            proc.StartInfo.RedirectStandardOutput = true;
            proc.StartInfo.RedirectStandardError = true;
            proc.StartInfo.CreateNoWindow = true;
            proc.Start();
            string dosLine = "net use " + path + " " + passWord + " /user:" + userName;
            proc.StandardInput.WriteLine(dosLine);
            proc.StandardInput.WriteLine("exit");
            while (!proc.HasExited)
            {
                proc.WaitForExit(1000);
            }
            string errormsg = proc.StandardError.ReadToEnd();
            proc.StandardError.Close();
            if (string.IsNullOrEmpty(errormsg))
            {
                Flag = true;
            }
            else
            {
                throw new Exception(errormsg);
            }
        }
        catch (Exception ex)
        {
            throw ex;
        }
        finally
        {
            proc.Close();
            proc.Dispose();
        }
        return Flag;
    }

    /// <summary>
    /// 向远程文件夹保存本地内容，或者从远程文件夹下载文件到本地
    /// </summary>
    /// <param name="src">要保存的文件的路径，如果保存文件到共享文件夹，这个路径就是本地文件路径如：@"D:\1.avi"</param>
    /// <param name="dst">保存文件的路径，不含名称及扩展名</param>
    /// <param name="fileName">保存文件的名称以及扩展名</param>
    public static string Transport(string fileName)
    {
        var domain = @"\\192.168.*.*\cost_model";
        bool loggedOn = connectState(domain, @"DLauto\**", "DLauto@2***");

        var src = Path.Combine(domain, fileName);
        var dst = @"D:\scb";

        if (loggedOn)
        {
            FileStream inFileStream = new FileStream(src, FileMode.Open);
            if (!Directory.Exists(dst))
            {
                Directory.CreateDirectory(dst);
            }
            dst = Path.Combine(dst, fileName);
            if (File.Exists(dst))
            {
                File.Delete(dst);
            }

            FileStream outFileStream = new FileStream(dst, FileMode.OpenOrCreate);

            byte[] buf = new byte[inFileStream.Length];

            int byteCount;

            while ((byteCount = inFileStream.Read(buf, 0, buf.Length)) > 0)
            {
                outFileStream.Write(buf, 0, byteCount);
            }

            inFileStream.Flush();
            inFileStream.Close();
            outFileStream.Flush();
            outFileStream.Close();
        }


        return dst;
    }

}
```



​	我这同事还有个问题请教我，就是他本地git pull 我的 branch 的代码，会有冲突，我帮他弄了，需要先stash change（储藏更改），再git pull，然后stash popup（弹出储藏），再解决冲突。

## 下午

​	下午我主要看了大模型的MCP（模型上下文协议）全称 Model Context Protocol，可以理解为是大模型用的 HTTP 传输协议，为Claude 发布，可以将 MCP 想象成 AI 应用程序的 USB-C 接口。就像 USB-C 为设备连接各种外设和配件提供了标准化的方式一样，MCP 为 AI 模型连接各种数据源和工具提供了标准化的接口。可以将 MCP 想象成 AI 应用程序的 Type-C 扩展坞（笔记本接口不够用的常用解决方案，我工作电脑就是用的这个，显示器是DP的，就需要扩展坞具有DP接口，鼠标键盘则为 Type-A 接口）。就像 Type-C 为设备连接各种外设和配件提供了标准化的方式一样，MCP 为 AI 模型连接各种数据源和工具提供了标准化的接口。然后还了解到了C# 有了可以用的SDK [modelcontextprotocol/csharp-sdk: The official C# SDK for Model Context Protocol servers and clients. Maintained in collaboration with Microsoft.](https://github.com/modelcontextprotocol/csharp-sdk)（dotnet add package ModelContextProtocol --prerelease），MCP架构采用客户端（Client）-服务器（Server）架构，主机（Host）应用可以连接多个服务器，也就是Host当中含有多个Client，Client与Server是一对一的关系。如下图所示：

![image-20250717232546191](https://rustfs.wenyongdalucky.club:443/astro-paper/image-20250717232546191.png)

​	还在Github上找了个 开源项目示例 [softlgl/McpDemo: Mcp学习示例](https://github.com/softlgl/McpDemo)



​	临近下班还解决两个ERP相关的bug，其中一个是B端的，为投产类别偶尔不显示多语言的情况，其实挺奇怪的，但发现@Html.loadtemplate中少了投产类别的字典类型，于是补充上解决的，由于解决完就去改另一个bug了，没有仔细看为什么会偶尔不显示多语言。之后又去解决了C端ERP的外购件投产单变更序时簿的表格上部分列名的多语言显示不全，为Deepl翻译导致的问题，中文里有（）就会导致这个问题。

![image-20250718095855504](@/assets/images/daily/2025-07-17/image-20250718095855504.png)

![image-20250718095912787](E:\Documents\个人文档\日记\2025.07.17\image-20250718095912787.png)

​	下班先回宿舍看了小红书上的 斐波那契数列究竟有多神奇、尼莫点究竟有多可怕、达芬奇最后的晚餐,到底隐藏了多少秘密、圆周率会被算尽吗、世界上最接近神的人 尼古拉·特斯拉、一鲸落，万物生 最浪漫的死亡与新生（看到这个想起了我的前女友让我给她的微信备注就是 鲸落，当时的我还不知道她让我备注这个是什么意思，但我现在知道了，原来是鲸鱼牺牲自己，成就了万物的新生，而我也是在她的陪伴下，迎来了新生）， 之后出去跑了步，主要还是竞走，（期间想了很多问题，试图把斐波那契数列的兔子的繁衍想明白，1+1+2+3+5+8。以及打算从今天起开始写日记，想着用笔写还是键盘打字，但我感觉键盘打字可以用Markdown格式，能记录图片等信息，然后打算是从Github、Gitee上找一个能用的Astro的开源博客），走完又去了下办公室，从冰箱取了葱油汁，晚饭是葱油汁拌荞麦面，还有家里拿的咸鸭蛋，但这么吃确实是有点儿咸了，吃完就喝了一整瓶矿泉水。又去了平日单位里再宿舍经常聊天的人的宿舍找他聊天，之后洗完澡回到屋子， 搜了搜能用的开源Astro 博客，开始写日记。