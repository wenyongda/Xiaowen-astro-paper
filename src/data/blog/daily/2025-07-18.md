---
author: 文永达
pubDatetime: 2025-07-18T22:25:54.547Z
title: 2025.07.18
featured: false
ogImage: https://github.com/satnaing/astro-paper/assets/53733092/1ef0cf03-8137-4d67-ac81-84a032119e3a
tags:
  - release
description: "我的日记"
---



# 2025-07-18

## 上午

​	今天上午我整理了下昨天写的日记中的遗漏的要点，然后我同事大哥他们组的辉哥找我帮他那台远程的Windows 10服务器上的Visual Studio 2019 Community 激活，因那台电脑无法联网，所以无法通过常规登录微软账号进行激活，于是我在bing上搜索方案，在csdn的这篇文章[【VS工具】社区版VS2019离线断网场景到期激活、延长试用期解决方法_vs2019社区版到期离线激活-CSDN博客](https://blog.csdn.net/PonderZhou/article/details/137610978)[【VS工具】社区版VS2019离线断网场景到期激活、延长试用期解决方法_vs2019社区版到期离线激活-CSDN博客](https://blog.csdn.net/PonderZhou/article/details/137610978)上找到了Github上有个开源项目[beatcracker/VSCELicense: PowerShell module to get and set Visual Studio Community Edition license expiration date in registry](https://github.com/beatcracker/VSCELicense)简单看了下是个powershell的脚本项目

有如下命令：

查看所有当前安装的Visual Studio Community版的剩余过期天数

```powershell
Get-VSCELicenseExpirationDate
```

指定一个版本

```shell
Get-VSCELicenseExpirationDate -Version 2017
```

多个版本的

```powershell
Get-VSCELicenseExpirationDate -Version 2019, 2017
```

设置延长所有当前安装的Visual Studio Community版的剩余过期天数为31天

```powershell
Set-VSCELicenseExpirationDate
```

指定一个版本

```powershell
Set-VSCELicenseExpirationDate -Version 2017
```

多个版本的

```powershell
Set-VSCELicenseExpirationDate -Version 2019, 2017
```

如本机是2019的，则如下命令延长31天

```powershell
Get-VSCELicenseExpirationDate

Version ExpirationDate
------- --------------
2019    18/07/2025 00:00:00


```

然后需要比较ExpirationDate的值是否和当前日期距离不到1天，如果是的话，则执行以下命令 Set-VSCELicenseExpirationDate -Version 2019 其中Version就是对应上面的版本号

由于看了下每次过期是需要手动执行的，为了达到自动效果，需结合任务计划程序，任务计划程序调用如上命令组合，并从第一个命令获取到Version对应的2019和ExpirationDate 对应的18/07/2025 00:00:00，需编写powershell脚本，由于我没接触过powershell脚本，于是这里AI就排上用场了，使用Qwen3-235B-A22B模型，开启思考模式，生成如下脚本：

```powershell
# 加载 VSCELicense 模块
try {
    Import-Module -Name 'C:\VSCELicense\VSCELicense.psd1' -ErrorAction Stop
} catch {
    Write-Error "无法加载模块 'C:\VSCELicense\VSCELicense.psd1'，请确认路径是否正确。错误信息: $_"
    exit 1
}


# 获取许可证信息
try {
    $output = Get-VSCELicenseExpirationDate -ErrorAction Stop | Out-String
} catch {
    Write-Error "执行 Get-VSCELicenseExpirationDate 时出错: $_"
    exit 1
}

# 使用正则表达式提取版本号和过期日期
$version = $null
$expDateStr = $null

if ($output -match '(?m)^\s*(\d{4})\s+(\d{2}/\d{2}/\d{4}\s+\d{2}:\d{2}:\d{2})') {
    $version = $matches[1].Trim()
    $expDateStr = $matches[2].Trim()
    Write-Host "找到版本: $version"
    Write-Host "过期日期: $expDateStr"
} else {
    Write-Error "无法从输出中找到版本和过期日期"
    exit 1
}

# 将过期日期字符串解析为 DateTime 对象
try {
    $expirationDate = [datetime]::ParseExact($expDateStr, 'dd/MM/yyyy HH:mm:ss', $null)
} catch {
    Write-Error "日期格式解析失败: $expDateStr. 错误: $_"
    exit 1
}

# 获取当前时间并设置阈值
$currentTime = Get-Date
$threshold = $currentTime.AddDays(1)

Write-Host "当前时间: $currentTime"
Write-Host "过期时间: $expirationDate"
Write-Host "阈值时间（当前时间 + 1天）: $threshold"

# 判断是否在1天内过期
if ($expirationDate -le $threshold) {
    Write-Host "许可证即将过期，正在执行 Set-VSCELicenseExpirationDate 命令..."
    try {
        Set-VSCELicenseExpirationDate -Version $version -ErrorAction Stop
        Write-Host "Set-VSCELicenseExpirationDate 命令执行成功。"
    } catch {
        Write-Error "执行 Set-VSCELicenseExpirationDate 时出错: $_"
        exit 1
    }
} else {
    Write-Host "许可证未在一天内过期，无需操作。"
}
```

需使用管理员开启Windows脚本执行

该脚本，我用了 之前说的Github 上的这个开源项目 fork 到我自己的repository里，之后新建了个branch起名 auto，寓意自动化

并将脚本上传进去，文件为 [Check-VS-LicenseExpiration.ps1](https://github.com/wenyongda/VSCELicense/blob/auto/Check-VS-LicenseExpiration.ps1)

## 下午

​	元坤叫我去会议室参加CATIA AI讨论会，技术中心总监权总来我们会议室讨论这个，首先小陈先介绍了下我们这段时间对于CATIA与AI大模型结合的工作成果，选择了个零件， 使用开发好的CAA插件，输入 改成白色，调用模型的API进行改色，权总说看下背后实现原理，小陈一时不知道如何展示，我告诉他打开Dify AI应用平台，用的我的账号和密码，但我不知道这个用的是哪个应用，元坤告诉了我，并打开工作流展示，我看里面用了 catia.txt 这个知识库文件，里面文件结构是 要让AI模型做的动作，这个是键，值是对应的json，json里有name、params属性，name代表要调用CAA里的方法名，params是参数，是要传递给这个方法里的参数，这个改色的name是changeColor，params 分别是 r g b。模型会根据传入的请求，进行生成，当然还需要做返回值的处理，只截取json串。权总说这种方式其实不是想要的，他想要的是读取当前CAAProduct展示的项目工程，然后读取各零件的参数，传递给大模型，这可以让大模型有个对于当前工程的认识，然后再给模型提需求，模型改对应参数就可以了，这个想法是很好的，但是实现起来难度很大，首先读参数，理论上可以通过CAA二次开发插件进行读取，但是模型也需要理解这些参数都是什么意思，这就需要知识库，甚至整理数据集进行训练来支撑。然后他又说了text2CAD这个项目，但我也是第一次听说，我后来查了下，就是通过文本生成CAD图型，这个需求也与AI要做的事儿很像，但是没法用在CATIA上，根本原因就是CATIA太专有了，它的工程文件都是二进制的，不是文本文件没法给模型去读取理解。权总还说了VsCode里的Github Copilot也就是小机器人，他还花10美元每个月买了，我以前用这个都是买的学生包，想起来还给我前女友也安排上了，权总他还用了claude 4，其实底层还是MCP，但我听他说这个还能给电脑安装python环境，如果之前没有的话，这点儿来看MCP是很强大的。开完会我、元坤、大闫还讨论了下是否还有别的方法，他提到了用xml进行描述，但其实都一样，都得让大模型去理解其中的含义。真正能做出CATIA能用的AI，也就是达索自己，毕竟自己知道自己的源代码，也会更好的生成数据集去训练对应的模型，但大闫认为如果达索出来了，那个模型也是通用的，不能专注于公司当前的白车身生产线项目，但我认为，大模型是可以专注于各个领域的，用对应数据集训练即可，就像微调一样。然后后来还聊到了电影，大闫说有个红皇后，但他不知道电影名是啥，其实就是生化危机里的红皇后，蜂巢意识，然后我就又说了个电影，我小时候最爱看的，我，机器人，我还给他俩推荐了星际穿越这个电影，我说这个可以看出很多很多的科学和哲理，是我最爱看的电影。回到工位后，大闫又拿自己的水果刀切了单位工会发的还剩一半的西瓜，但这次我没去卫生间的洗手盆吃，我直接在切西瓜那位置直接吃了，但我觉得这样会让地很黏。大闫说他会拿拖布拖。我回到工位上看了看Astro框架，就我现在的日记的基础框架。然后又看了MCP的底层协议：JSON-RPC。我以前的认知json就是rest、http请求，但现在了解了还有一种规范，就像gRPC一样，其实rpc就是规范。下班我和刁哥、李姐表达了我想去德国的想法，但是我的业务水平不行，知道ERP里的业务太少了，我决定在这一年跟李姐学习业务，然后再看代码去理解这个，多写点儿注释，再结合我的日记，去加深印象。

​	下班了和我宿舍之前一起喝酒的去坐公交，本来要去旅顺市内，结果等来了对面的205，他于是提议去大外，我答应了，然后我看205是饶了一圈才去的大外，我也是第一次坐这个方向的车，我俩逛了一下，他先去买的鸡架，然后就去吃了自助烧烤，自助烧烤看起来很不错，那个炉子是炭火，但是有放铁签的槽，然后利用签子尾部的齿轮，通过炉子两边的槽，左右移动带动签子旋转，这个设计确实很好，省去了烧烤时需要不停的拿手去转签子，老板娘很热情，过一会就来看一下我们这是否有烤熟的，烤熟的就放在炉子上面那个不会动的杆子上。

![image-20250718235922687](C:\Users\12241\AppData\Roaming\Typora\typora-user-images\image-20250718235922687.png)

​	其中坐在他后面那桌有个大叔，我感觉他的眼界真的很高，把当今社会的存在的问题都说了出来，还时不时看着我，我也很同意他的说法，其实比较后悔后面没加他的微信。我俩吃完就出去，往南走一走就是盐场，海边，我提议去看看，他说那都是装海带的，但我不这么认为，既然都到了，何不看看呢，于是去看了下海，他还不忘记给媳妇打视频让她和儿子一起看看海。而我不停的望着海，看到了海上游得海鸭，这是我第一次看到海鸭，感觉和淡水鸭没什么两样，然后我还想起了海鸭蛋很好吃。我看到了周围的渔船、又静下心听了下海的声音，海浪敲击沙滩的声音，这是天然的白噪音，很助眠。然后我俩就沿着道去了大外西门，刚好来了辆可以回单位的205

![image-20250719000507978](C:\Users\12241\AppData\Roaming\Typora\typora-user-images\image-20250719000507978.png)

​	然后回来，先去办公室取了我的包，之后回了宿舍，因为吃的比较多，我怕自己降下来的体重又弹回去，但考虑到跑步会造成胃下垂的风险，于是决定了采取竞走的方式替代。等都跑完了，回宿舍，搁淘宝上买了 杜比音效兑（dui）换码，但是我自己怎么都激活不了，改地区、挂加速器、改host、机场都安排上了，还是不行，一直提示该激活码与账号绑定地址不同，后面有些抓狂，首先把我微软账户改了下密码，临时密码，丢给了客服，客服后来跟我说是官方服务故障，看来不是我的问题，我甚至因此怀疑自己，他说出这话，我就相信了这不是我的问题。

![image-20250719001231337](C:\Users\12241\AppData\Roaming\Typora\typora-user-images\image-20250719001231337.png)

![image-20250719001102377](C:\Users\12241\AppData\Roaming\Typora\typora-user-images\image-20250719001102377.png)
